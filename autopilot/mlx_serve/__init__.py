"""Local Apple-Silicon serving via mlx-lm: an OpenAI-compatible endpoint for the
distilled student, and a sub-LM client the RLM checks call. Stdlib-only at import
time (mlx is only needed to actually run the server)."""
